arr = [[3, 6], [7, 8], [2, 9], [6, 4], [9, 3]]

score = 0
for a, b in arr:
    result = int(input(f'{a} * {b} = ? '))
    if result == a * b:
        score += 20
        print("맞았습니다.")
    else:
        print("틀렸습니다.")

print("최종 점수는", score, "점입니다.")
