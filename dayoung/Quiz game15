import random
score = 0

for _ in range(5):
    a = random.randint(2, 9)
    b = random.randint(1, 9)
    result = int(input(f'{a} * {b} = ? '))
    if result == a * b:
        score += 20
        print("맞았습니다.")
    else:
        print("틀렸습니다.")

print("최종 점수는", score, "점입니다.")
