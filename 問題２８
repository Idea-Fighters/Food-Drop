T = int(input())

# T번 반복
for _ in range(T):
    # 문자열 입력
    input_str = input()
    
    # 첫 글자와 마지막 글자 추출 및 출력
    first_char = input_str[0]
    last_char = input_str[-1]
    print(first_char + last_char)
