while True:
    # 두 정수를 입력받음
    a, b = map(int, input().split())

    # 입력이 0 0 이면 반복문을 종료
    if a == 0 and b == 0:
        break

    # a가 b보다 크면 "Yes" 출력, 아니면 "No" 출력
    if a > b:
        print("Yes")
    else:
        print("No")
